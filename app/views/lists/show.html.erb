<h2><%=@list.name%> list</h2>



<% @list.bookmarks.each do |x| %>
      <li>Your bookmark: "<%= x.comment %>, <%= x.movie.title %>, <%= link_to "Delete a bookmark", bookmark_path(x), data: {turbo_method: :delete} %> </li>
  <% end %>


<%= link_to "Delete a list", list_path(@list), data: {turbo_method: :delete} %>
<%= link_to "Back to lists", "/lists" %>
